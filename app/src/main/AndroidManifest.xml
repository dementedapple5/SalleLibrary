<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dementedapple5.sallelibrary">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_salle_library"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_salle_library_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".userauth.activities.LoginActivity" />
        <activity
            android:name=".mainmenu.TabbedActivity"
            android:label="@string/title_activity_tabbed"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".splashscreen.activities.SplashScreenActivity"
            android:theme="@style/SplashTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mainmenu.TabbedActivity"
            android:label="@string/title_activity_tabbed"
            android:theme="@style/AppTheme" />
        <activity android:name=".mainmenu.BookDetailsActivity"/>
        <activity android:name=".bookpage.activities.BookPage">
        </activity>
    </application>

</manifest>